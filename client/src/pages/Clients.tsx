export default function Clients() {
  return (
    <div className="main">
      <h1 className="page-title">Clients</h1>
      <p className="page-subtitle">Not built yet.</p>
      <div className="card">
        <p style={{ marginTop: 0, color: "var(--ink-dim)", fontSize: 13 }}>
          Clockify lets you group projects under billing clients. That's not wired up here yet — no
          <code style={{ margin: "0 4px" }}>clients</code> table, no assignment on projects. It's kept
          in the sidebar (behind a toggle in Settings) as a placeholder; say the word if you actually
          want client tracking built out and it can become a real page.
        </p>
      </div>
    </div>
  );
}
