node_modules/
dist/
build/
*.log

# Local database — this is the user's owned data, never committed
server/data/*.db
server/data/*.db-journal
server/data/*.db-wal
server/data/*.db-shm

# Secrets
.env
server/.env

# Editor / OS
.DS_Store
.vscode/
